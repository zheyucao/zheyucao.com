---
import Hero from "../Hero.astro";
import type { HeroSection } from "../../../lib/homepageSections/registry";

interface Props {
  section: HeroSection;
}

const { section } = Astro.props;
---

<Hero data={section} />
